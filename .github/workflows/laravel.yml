name: Laravel

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: []

jobs:
  laravel-tests:

    runs-on: ubuntu-latest

    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@master
      with:
          username: ${{ secrets.USERNAME }}
          host: "52.187.66.134"
          password: ${{ secrets.PASSWORD }}
          script: 'cd ~ && touch ssh from github action'
